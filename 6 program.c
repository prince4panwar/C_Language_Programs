//write a program to calculate area of circle take radius as an input from user
main()
{
    float r, area;
    printf("Enter the radius of circle:-");
    scanf("%f",&r);
    area=3.14*r*r;
    printf("Area of circle is %f sq units",area);
    getch();
}
