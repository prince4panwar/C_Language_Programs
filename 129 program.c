//Write a program to calculate length of the string by using strlen function.
void main()
{
    char str[20];
    int l;
    printf("Enter a string :- ");
    gets(str);
    l=strlen(str);
    printf("Length of the string is %d",l);
}
